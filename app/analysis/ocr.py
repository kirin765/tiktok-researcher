from __future__ import annotations

from typing import Any


def extract_ocr(path: str) -> list[dict[str, Any]]:
    # Optional OCR stub; if OCR dependency isn't installed we keep empty.
    return []
